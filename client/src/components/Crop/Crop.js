import React from "react";
import "./Crop.css";

/* TODO: We need to pass in a name, a picture, and optionally a difficulty */

const Crop = (props) => (    
    <div className="crop">
        
    </div>
);

export default Crop;
